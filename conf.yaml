#database configuration
user: "root"
password: "avito"
host: "172.19.0.1"
port: "3306"
db: "db"

#replication configuration
name: "default"
copy: 3
tables_string: ["CREATE TABLE users (id NOT NULL, Name VARCHAR(255))",
                "CREATE TABLE passwords (id NOT NULL, Password VARCHAR(255))",
              ]
